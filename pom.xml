<project>
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>walkingdevs</groupId>
        <artifactId>archetypes-parent</artifactId>
        <version>9</version>
        <relativePath/>
    </parent>

    <artifactId>tomee-war</artifactId>
    <version>7.1</version>
    <packaging>maven-archetype</packaging>

    <build>
        <resources>
            <resource>
                <directory>main</directory>
            </resource>
        </resources>

        <testResources>
            <testResource>
                <directory>test</directory>
            </testResource>
        </testResources>
    </build>

    <repositories>
        <repository>
            <id>bintray</id>
            <url>http://dl.bintray.com/walkingdevs/mvn</url>
        </repository>
    </repositories>
</project>